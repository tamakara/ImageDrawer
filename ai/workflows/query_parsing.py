def query_parsing_workflow(query: str) -> dict:
    """
    Query parsing workflow.
    - Extract semantic keywords
    - Generate candidate tags
    - Match with database tags
    - Output structured search condition
    """

    # TODO: LangChain parsing
    # TODO: keyword expansion
    # TODO: database tag matching

    # placeholder
    return {
        "include_tags": ["cat", "orange_cat"],
        "exclude_tags": [],
        "logic": "AND"
    }
